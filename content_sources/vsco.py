def fetch_vsco_links(limit=5):
    # Placeholder: simulate VSCO image links
    return [
        {"title": f"VSCO Art {i+1}", "url": f"https://vsco.co/gallery/post{i+1}", "type": "image"}
        for i in range(limit)
    ]
